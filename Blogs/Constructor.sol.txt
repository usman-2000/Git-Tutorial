Constructor is basically a special type of function. it is used when you want to deploy your smart contract with something like that remain same for the whole smart contract and can never change. It is mostly used when you want to give authority to an admin or owner. Constructor can only be deploy once and canot be called inside any function. But it can call any function inside it. It can also get an arguments. It is actually a function that's why it has a braces.
.....